[defaults]
roles_path = roles
retry_files_enabled = False
host_key_checking = True
result_format = yaml
forks = 20

[ssh_connection]
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s
